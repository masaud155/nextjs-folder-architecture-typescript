export const routes = {
  home: "/",
  marketing: "/marketing",
  pricing: "/pricing",
  login: "/login",
  register: "/register",
  dashboard: "/dashboard",
  settings: "/settings",
  api: {
    health: "/api/health",
    billing: "/api/billing"
  }
} as const;
